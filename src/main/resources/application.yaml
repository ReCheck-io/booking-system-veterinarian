server:
  error:
    include-message: always
    include-binding-errors: always
    include-exception: false

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/bookingvetdb
    initialization-mode: always
    separator: ^
    username: app_dev_loc
    password: app_dev_loc_opx07
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true